{
  "name": "slot-machine-cs",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm run dev -w @slot/client",
    "dev:server": "npm run dev -w @slot/server",
    "build": "npm run build -ws",
    "typecheck": "npm run typecheck -ws"
  }
}
